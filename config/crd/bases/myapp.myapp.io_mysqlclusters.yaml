---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mysqlclusters.myapp.myapp.io
spec:
  group: myapp.myapp.io
  names:
    kind: MySQLCluster
    listKind: MySQLClusterList
    plural: mysqlclusters
    singular: mysqlcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.masterPod
      name: Master
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: MySQLCluster is the Schema for the mysqlclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MySQLClusterSpec defines the desired state of MySQLCluster
            properties:
              ImagePullPolicy:
                description: 镜像拉取策略
                type: string
              StorageClassName:
                description: storageclass
                type: string
              env:
                additionalProperties:
                  type: string
                description: 环境变量
                type: object
              image:
                default: mysql:8.0
                description: MySQL 镜像
                type: string
              port:
                description: 端口
                format: int32
                type: integer
              replicas:
                default: 3
                description: 副本数（1 主 + N 从）
                format: int32
                minimum: 1
                type: integer
              rootPasswordSecret:
                description: Root 密码（从 Secret 引用）
                type: string
              storageSize:
                default: 10Gi
                description: 存储大小
                type: string
            type: object
          status:
            description: MySQLClusterStatus defines the observed state of MySQLCluster
            properties:
              StatefulSetName:
                type: string
              lastUpdated:
                type: string
              masterPod:
                type: string
              message:
                type: string
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
              slavePods:
                items:
                  type: string
                type: array
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
